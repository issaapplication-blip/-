const CACHE="rafig-v14-official-logo";
const APP_SHELL=["/","/manifest.webmanifest","/rafig-approved-logo.jpg","/rafig-approved-logo-192.jpg","/rafig-approved-logo-512.jpg"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(APP_SHELL).catch(()=>{})));self.skipWaiting();});
self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))));self.clients.claim();});
self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;const url=new URL(event.request.url);const fresh=["/","/index.html","/manifest.webmanifest","/rafig-approved-logo.jpg","/rafig-approved-logo-192.jpg","/rafig-approved-logo-512.jpg"].includes(url.pathname);event.respondWith(fetch(event.request,{cache:fresh?"no-store":"default"}).then(response=>{if(response.ok)caches.open(CACHE).then(cache=>cache.put(event.request,response.clone())).catch(()=>{});return response;}).catch(()=>caches.match(event.request).then(cached=>cached||caches.match("/"))));});
